<div class="card">
  <div class="org-logo">
    <img class="org-logo__image" src="{{logo}}" alt="Company Logo" />
  </div>
  <h1 class="heading">
    {{#if canceledTitle}}
    {{canceledTitle}}
    {{else}}
    {{i18n 'canceled_title'}}
    {{/if}}
  </h1>
  <div class="text-block">
    <div class="text-block__text">
      {{#if canceledMessage}}
      {{canceledMessage}}
      {{else}}
      {{i18n 'canceled_text'}}
      {{/if}}
    </div>
  </div>
</div>